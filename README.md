## LLM Powered Postpartum Depression Alert System

### How to run?
* Create a virtual environment with venv (`python -m venv .venv`)
* run `pip install -r requirements.txt`
* run `python manage.py runserver`